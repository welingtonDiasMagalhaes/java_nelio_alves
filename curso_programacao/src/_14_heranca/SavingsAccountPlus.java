package _14_heranca;

public class SavingsAccountPlus extends SavingsAccount {
	/*
	 * não é permitido devido ao final
	 * @Override public void withdraw(Double amount) { balance -= amount -2; }
	 */
}
